[project]
name = "sample-public-app"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "boto3>=1.34",
    "neo4j>=6,<7",
]

[project.scripts]
sample-public-app = "sample_public_app.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
